<div id="initiative-<%= initiative.id %>-supports-count" class="card__support__data">
  <% unless initiative.initiative_votes_count.zero? %>
    <span class="card__support__number"><%= initiative.initiative_votes_count %></span>
    <span><%= t('.count', count: initiative.supports_count) %></span>
  <% end %>
</div>

